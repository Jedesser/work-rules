# <Название задачи> (#<номер задачи>)

Статус: <заменить эту строку целиком на один из двух вариантов ниже>
<!-- Допустимы ровно два варианта, дословно:
       «Статус: согласован владельцем ГГГГ-ММ-ДД» — дата, когда владелец ответил
       «Статус: технический — согласование не требуется»
     Проверка читает первую строку, начинающуюся с «Статус: », и сравнивает
     её побайтово. Пока строка не заменена, план считается незаполненным —
     это намеренно: непонятно, ждала ли работа решения человека. -->

## Что изменится для пользователя

<!-- Для владельца. 3–5 предложений обычным языком, без имён файлов, пакетов и
     технологий. Что человек увидит или перестанет видеть. Если задача чисто
     внутренняя и снаружи не заметно ничего — так и написать одной строкой.
     Это единственный блок, по которому нетехнический человек может понять,
     то ли вы собираетесь строить. -->

## Что меняем технически

<!-- Рабочий блок. Какие модули трогаем, в каком порядке и почему выбрано это
     решение, а не соседнее.
     Порядок — ВЕРТИКАЛЬНЫМИ СРЕЗАМИ: один сквозной кусок, доведённый до
     проверяемого состояния, потом следующий. «Сначала весь бэкенд, потом весь
     фронт» — это порядок, при котором ничего нельзя проверить до самого конца. -->

## Как поймём, что работает

<!-- Ссылка на поле «Готово, когда» из задачи: «см. #<номер>». Условие живёт в
     одном экземпляре, чтобы две формулировки не разошлись. Если условие по ходу
     изменилось — правим в задаче и упоминаем об этом ниже. -->

## Открытые вопросы

<!-- Для владельца. Всё, что требует его решения, — одним списком и ДО начала
     работы, а не по одному вопросу посреди дела. Ответ дописывается сюда же:
     «РЕШЕНО <дата>: <ответ>», исходный вопрос остаётся видимым.
     Если вопросов нет — написать «нет» и назвать сделанные допущения.
     Молчаливое допущение — ровно то, что этот блок должен предотвращать. -->

## Что может сломаться

<!-- Что стоит рядом и может пострадать: смежные экраны, фоновые задачи, уже
     накопленные данные, поведение при плохой связи. Если риск известен и
     принят — сказать об этом здесь, а не умолчать. -->

## Развилки замысла

<!-- Что изменилось в замысле по ходу работы и почему: выбранная развилка,
     отвергнутый вариант, вскрывшееся ограничение. Отсюда следующая сессия
     поднимает контекст, не перечитывая код.
     НЕ статус и НЕ журнал: «сделано то-то», «застряли на том-то», блокеры и
     передача работы идут в задачу.
     Причина жёсткая: план — часть диффа, и любая правка этого файла обнуляет
     расписки всех ревьюеров. Поэтому последняя правка плана делается ДО
     запуска ревьюеров, а всё выясненное после — в описание PR. -->
